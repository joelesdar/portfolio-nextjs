import styled from '@emotion/styled'

export const StyledBanner = styled.section`
  background: var(--dark);
  padding: 160px 0;

  .title {
    text-align: center;
    color: var(--main-blue);
    margin: 0;
  }

  .description {
    margin: 32px 0 64px 0;
  }

  .cards-container {
  }

  .carrousel-container {
  }

`
